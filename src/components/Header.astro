---
import i18next, { t, changeLanguage } from "i18next";
import { Trans, HeadHrefLangs } from "astro-i18next/components";
---


<header class="flex justify-center items-center py-5 w-full xl:w-[1120px] mx-auto">

    <nav class="hidden sm:flex flex-row gap-x-10 opacity-80">
        <a class="hover:scale-105 transition" href="#home">{t("header.home")}</a>
        <a class="hover:scale-105 transition" href="#about">{t("header.about")}</a>
        <a class="hover:scale-105 transition" href="#certificates">{t("header.certificates")}</a>       
        <a class="hover:scale-105 transition" href="#projects">{t("header.projects")}</a>
        <a class="hover:scale-105 transition" href="#contact">{t("header.contact")}</a>
    </nav>
</header>

