/** @type {import('astro-i18next').AstroI18nextConfig} */
export default {
    defaultLocale: "en",
    locales: ["en", "es", "pt", "it", "de", "fi"],
    // currentLocale: process.env.LOCALE || navigator.language.slice(0, 2), 
  };